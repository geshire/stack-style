import React from "react";
import PropTypes from "prop-types";

const Tabs = ({ }) => (
  <div className="tabs">
    <nav>
      <ul>
        <li>Nav Item</li>
      </ul>
    </nav>

    <section>
        Tab
      </section>
  </div>
  );

Tabs.propTypes = {
};

Tabs.defaultProps = {
};

export default Tabs;
